# 环境标识
VITE_BUILDENV=test

# API 基础路径
# 约定：生成的 OpenAPI 请求路径已包含 /api 前缀，因此这里保持为 '/'
VITE_API_BASE_URL=/

# Sentry 配置 (测试环境)
VITE_SENTRY_ENABLE=true
VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
VITE_SENTRY_ENV=test
