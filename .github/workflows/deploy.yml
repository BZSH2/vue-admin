name: Deploy CI/CD (manual)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is kept for manual use only. Server deployment uses deploy-aliyun.yml."
